# Phase 32 — Simulator UX

`/simulator` sections: 1. Race selection (season input + `GET /races`, detail via `GET /races/{id}`; loading/empty/error states). 2. Mode radio Single Race / Monte Carlo (Strategy Lab, Counterfactual, Championship, Regulation Lab shown disabled). 3. Configuration (seed int32, laps 1..200, N 1..5000 with "Maximum 5,000" label; 4 modifier checkboxes from backend schema). 4. Evidence/model status (`GET /metadata` tiers). 5. Run + `JobProgress` (status/stage/backend progress only) + error alert. 6. Results: single-race table (Pos/Driver/Constructor/Time), Monte Carlo top-10 win/podium/expected with neutral interpretation text; `SimulationProvenance`; Set-as-A/B + Rerun; `ComparisonView` shows observed Δ only ("not causal attribution"). Desktop/tablet-first; semantic buttons/labels, focus-visible via Tailwind, aria-labels on polling status and badges, no color-only meaning.
